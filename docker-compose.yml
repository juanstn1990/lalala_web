services:
  lalala:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro

  api:
    build: ./api
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
